spring:
  application:
    name: store
  datasource:
    url: jdbc:myslq://localhost:3306.store?createDatabaseIfNotExist=true
    username: root
    password: MyPassword!
stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 1000
  supported-currencies: GBP,EUR,USD
payment-gateway: paypal

mail:
  host: http://smtp.gmail.com
  port: 5761
